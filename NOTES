## 🧠 A Verifier
- L'utilisation du point virgule (;) lors d'une commande.
- L'expansion + reconnaissance de la variable dans une chaine.

## 🧪 Tableau de tests

| Catégorie | Input (USER=john) | Résultat attendu |
|-----------|-------------------|------------------|
| **Collée texte** | ❌ `echo ABC$USERDEF` | `ABCjohnDEF` |
| **Heredoc délimiteur** | ❌ `<< $EOF` | Tout afficher comme un cat |
| **Redir multiple** | `echo a > f1 > f2` | Les fichier `f1` et `f2` contient `a` |
| **Multi heredoc** | `cat << A << B` | Seul le dernier est utilisé (verifier l'affichage)|
